# DO NOT EDIT: This file should only be modified in the `go-library-template` repo.

name: Setup Go
description: |
  This action sets up Go for use in actions by:
  - extracts the Go version from the go.mod file
  - runs the setup-go action to install the specified Go version
  - for private repos then:
    - creates a GitHub App Token to authenticate with the GitHub API
    - generates git config files to authenticate with the GitHub API
    - runs go mod download to install dependencies
    - removes the git token from the cache

inputs:
  private-key:
    description: >
      The private key for the GitHub App, needed to authenticate with the
      GitHub API to pull private go repos. For public repos this will
      be an empty string.
    required: true
  private:
    description: >
      If the repo is private or not.
    required: true
  owner:
    description: >
      The owner of the repo.
    required: true
  clear-token:
    description: >
      If the token should be cleared from the cache after the reusable
      workflow has completed. If setting to 'false' then you should manually
      call `git credential-cache exit` after you have finished with the token.
    default: 'true'
    required: false
  cache-suffix:
    description: >
      Optional suffix appended to the Go deps cache key. Use for jobs whose
      build cache content differs from the default (e.g. the race job, which
      compiles every package with -race and so produces distinct GOCACHE
      entries from non-race jobs). Without a per-job suffix, race and
      non-race jobs collide on the same cache key — only one save wins per
      run, leaving the loser's compile artefacts uncached on subsequent
      runs. Pass 'race' from the race job; leave empty for everything else.
    default: ''
    required: false

outputs:
  go-mod-version:
    description: Version of Go extracted from go.mod file
    value: ${{ steps.go-version.outputs.go-mod-version }}

runs:
  using: "composite"
  steps:
    - name: Extract Go version
      uses: arnested/go-version-action@6a9d1e0bcc9db5b1693bb7b0ad37491d205f67b6 # v2.1.4
      id: go-version

    # For libraries then we use the latest patch version of the minor go version.
    # This should not be used for services.
    - name: Split version code
      uses: step-security/split-strings@3f98629b8f82184c8b6de3199e16f1f68067f7ab # v1.0.2
      id: split
      with:
        string: ${{ steps.go-version.outputs.go-mod-version }}
        separator: .
        limit: 2

    - name: Set up Go
      uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
      with:
        go-version: '${{ steps.split.outputs._0 }}.${{ steps.split.outputs._1 }}'
        check-latest: true
        cache: false

    - name: Cache Go modules + build cache
      uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        path: |
          ~/go/pkg/mod
          ~/.cache/go-build
        key: setup-go-${{ runner.os }}-${{ runner.arch }}-go-${{ steps.go-version.outputs.go-mod-version }}${{ inputs.cache-suffix != '' && format('-{0}', inputs.cache-suffix) || '' }}-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          setup-go-${{ runner.os }}-${{ runner.arch }}-go-${{ steps.go-version.outputs.go-mod-version }}${{ inputs.cache-suffix != '' && format('-{0}', inputs.cache-suffix) || '' }}-
          setup-go-${{ runner.os }}-${{ runner.arch }}-go-${{ steps.go-version.outputs.go-mod-version }}-
          setup-go-${{ runner.os }}-${{ runner.arch }}-go-

    - name: Create a GitHub App Token
      uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
      if: ${{ inputs.private && inputs.private-key != '' }}
      id: app-token
      with:
        app-id: 1003800
        private-key: ${{ inputs.private-key }}
        owner: ${{ inputs.owner }}

    - name: Generate git config files
      if: steps.app-token.outcome == 'success'
      shell: bash
      run: |
        git config --global credential.https://github.com.helper cache

        git credential approve <<EOF
        protocol=https
        host=github.com
        username=x-access-token
        password=${{ steps.app-token.outputs.token }}
        EOF

    - name: Install dependencies
      shell: bash
      env:
        GOPRIVATE: github.com/PaddleHQ/*
      run: |
        go mod download ${{ runner.debug == '1' && '-x ' || '' }}all

    - name: Remove git token from cache
      if: inputs.clear-token == 'true'
      shell: bash
      run: git credential-cache exit
